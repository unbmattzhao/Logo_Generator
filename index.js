import fs from "fs";
import inquirer from "inquirer";
// import from;
// import from;
// import from;

inquirer
  .prompt([{}, {}, {}, {}])
  .then((answers) => {})
  .catch((error) => {
    if (error.isTtyError) {
    } else {
    }
  });
